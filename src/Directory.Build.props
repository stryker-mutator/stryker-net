<Project>

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
  </PropertyGroup>

  <PropertyGroup>
    <ManagePackageVersionsCentrally>true</ManagePackageVersionsCentrally>
    <RestorePackagesWithLockFile>true</RestorePackagesWithLockFile>
    <DisableImplicitNuGetFallbackFolder>true</DisableImplicitNuGetFallbackFolder>
    <ImplicitUsings>disable</ImplicitUsings>
    <VersionPrefix>4.16.0</VersionPrefix>
    <VersionSuffix></VersionSuffix>
    <RestoreLockedMode Condition="'$(RestoreLockedMode)' == 'true'">true</RestoreLockedMode>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="DotNet.ReproducibleBuilds" PrivateAssets="All" />
   <!-- We explicitly reference the package to overwrite the vulnerable version Microsoft.Build.Tasks.Core brings in -->
    <PackageReference Include="System.Security.Cryptography.Xml" PrivateAssets="All" />
  </ItemGroup>

</Project>
