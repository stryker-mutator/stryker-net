<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <!--
      The lower half of the stack. The test project references Calculator only, so Formatter is
      loaded transitively: one test host, two mutated assemblies, exactly the shape that loses
      coverage when the copies of the injected MutantControl share a single coverage file.
    -->
    <ProjectReference Include="..\Formatter\Formatter.csproj" />
  </ItemGroup>

</Project>
